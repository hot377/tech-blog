{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push:*)",
            "command": "DIR=\"${CLAUDE_PROJECT_DIR:-.}\"; PREV=$(git -C \"$DIR\" rev-parse origin/main@{1} 2>/dev/null); CUR=$(git -C \"$DIR\" rev-parse origin/main 2>/dev/null); if [ -n \"$PREV\" ] && [ \"$PREV\" != \"$CUR\" ]; then RANGE=\"$PREV..$CUR\"; else RANGE=\"HEAD~1..HEAD\"; fi; if git -C \"$DIR\" diff --name-only \"$RANGE\" 2>/dev/null | grep -q \"^articles/\"; then sleep 60 && git -C \"$DIR\" pull --rebase origin main 2>&1 | tail -20; else echo \"articles/ の変更なし。Qiita同期 pull をスキップ\"; fi",
            "async": true,
            "timeout": 120,
            "statusMessage": "push後のQiita同期を待機して pull --rebase（articles/変更時のみ）..."
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */articles/*) pgrep -f \"zenn preview\" >/dev/null 2>&1 || ( cd \"${CLAUDE_PROJECT_DIR:-.}\" && nohup npx zenn preview >/tmp/zenn-preview.log 2>&1 </dev/null & ) ;; esac; } 2>/dev/null || true",
            "statusMessage": "zenn preview サーバを起動（未起動時のみ）..."
          }
        ]
      }
    ]
  }
}
